<h2>Hello World!</h2>

<% content_for :head do %>
  <script type="javascript">
      $(function() {
        var App = <%= current_user %>;
        console.log(App);
        // toto(App);
      });
  </script>
  <% end %>

<%= render 'stats'%>

<%= render 'statsDetails', project: @project %>
