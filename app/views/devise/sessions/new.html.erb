<div class="row">
    <div class="col-md-4 col-md-offset-4">
        <p id="notice" class="text-info"><%= notice %></p>
        <p id="alert" class="text-danger"><%= alert %></p>

        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <%= f.error_notification %>

            <div class="form-inputs">
                <div class="form-group"><%= f.input :email, label: "Email", required: false, input_html: { class: "form-control" } %></div>
                <div class="form-group"><%= f.input :password, label: "Mot de passe", required: false, input_html: { class: "form-control" } %></div>

                <div class="form-group checkbox"><%= f.input :remember_me, label: "Se souvenir de moi", as: :boolean if devise_mapping.rememberable? %></div>
            </div>

            <div class="form-actions">
                <%= f.button :submit, "Connexion", class: "btn btn-primary" %>
                <%#<%= link_to "Mot de passe oublié ?", new_password_path(resource_name), class: "btn btn-link" %>
            </div>
        <% end %>
    </div>
</div>
